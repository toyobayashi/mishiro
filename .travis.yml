language: node_js
node_js:
  - '10.16.0'
os:
  - linux
  - osx
before_install:
  - node -v
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew install yarn --ignore-dependencies; fi
  - yarn --verison
  - cd app
  - yarn global add node-gyp@3
  - node-gyp install --target=4.2.6 --dist-url=https://atom.io/download/electron
install:
  - yarn
script:
  - yarn run pack:x64
branches:
  only:
  - master
