version: 1.4.3-{build}
image: Visual Studio 2017
install:
- cmd: >-
    node -v

    npm -v

    cd app

    npm install -g node-gyp

    node-gyp install --target=2.0.7 --dist-url=https://atom.io/download/electron

    npm install
build_script:
- cmd: npm run pkg64 install
test: off
